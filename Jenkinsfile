pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo Building Linux Server Monitor'
            }
        }

        stage('Test') {
            steps {
                sh 'echo Testing Application'
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo Deployment Complete'
            }
        }
    }
}
